<% # <h1> Selecione um veiculo para <%= @order.code %> 

<% # <%= form_with model: @order, url: order_path do |form| %>   %>
    <% # <%= form.label "Veiculo" %>
    <% # <%= form.collection_select :vehicle_id, @vehicles, :id, :license_plate %>   %>
    <% # <%= form.hidden_field :status, value: "accepted" %>   %>
    <% # <%= form.submit "Salvar" %>   %>   %>
<% # #<% end %>   %>   %>